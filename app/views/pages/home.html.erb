<div id="conteneur">
<div id="contenu">
  <h1><%= title %></h1>
  <div id="header"><p><i>Quel message veux-tu soutenir ?</i></p></div>
  <table border="0">
  <% @proposals.each do |proposal| %>
    <tr>  
      <td>
        <a href="<%= choice_path(proposal.id) %>">
          <p><strong><%= proposal.title %></strong> - <%= proposal.content %></p>
        </a>
      </td>
      <td><img src="<%= proposal.photo_url %>" width="90px"></img></td>
    </tr>
  <% end %>
  </table>  
  <%= render 'shared/ref' %>
</div>
 </div>
